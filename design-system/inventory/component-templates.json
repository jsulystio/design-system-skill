{
  "_comment": "Project-specific roadmap catalog. Add components you have AGREED to build but that are not in the Figma file or code yet — the build renders each (whose name is not already in components.json) as a 'planned' placeholder page and includes its spec in DESIGN-SYSTEM.md so coding agents scaffold the agreed API. The full AlignUI template catalog now lives in components.json.",
  "components": []
}
